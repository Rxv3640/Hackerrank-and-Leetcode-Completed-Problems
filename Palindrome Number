class Solution:
    def isPalindrome(self, x: int) -> bool:
        x = str(x)

        x2 = ''

        counter = -1

        for i in range(0, len(x)):

            x2 += x[counter]

            counter -= 1

        if x == x2:
            return True
        else:
            return False
